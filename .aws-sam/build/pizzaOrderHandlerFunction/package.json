{
    "name": "pizzaOrderService",
    "description": "pizza ordering service which transmits orders through SQS queue to DynamoDB",
    "version": "0.0.1",
    "private": true,
    "dependencies": {},
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest",
        "predeploy": "sam build",
        "deploy": "sam deploy --stack-name pizzaOrderService --resolve-s3 --capabilities CAPABILITY_IAM"
    }
}
